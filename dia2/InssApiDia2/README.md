# InssApiDia2 — gabarito da manhã (M7)

Continua o `InssApiTarde` do Dia 1.

Visual Studio 2026 → abrir esta pasta → F5 → `/swagger`

- Relação 1:N (Ana → pedidos)
- GET `/api/contribuintes/{id}/pedidos`
- GET `/api/pedidos?pagina=1&tamanho=2` (nome da Ana no JSON)
- POST pedido com transação
- POST Ana de novo → 400 NUIT já cadastrado
